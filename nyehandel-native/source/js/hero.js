/* Hazey — native hero behavior. NOT part of this delivery.
   Scaffolded per the requested export structure only — hero porting
   is explicitly out of scope this round (see nyehandel-native/README.md).
   build-native.js skips empty source files, so this contributes
   nothing to dist/ yet. */
