{
  "name": "hazey-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "Hazey.se (Nyehandel) storefront facelift — CSS + injected JS.",
  "scripts": {
    "build": "node build.js",
    "parity:update": "PARITY_MODE=update playwright test",
    "parity:update-impl": "PARITY_MODE=update-impl playwright test",
    "parity": "playwright test",
    "parity:report": "playwright show-report tests/results/html-report",
    "test:tema6": "playwright test tests/tema6-smoke.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "pixelmatch": "^5.3.0",
    "playwright": "^1.62.1",
    "pngjs": "^7.0.0",
    "terser": "^5.31.0"
  }
}
