.env
node_modules/
*.log
.DS_Store
scratchpad/

# Generated by `npm run parity` / `npm run parity:update` — regenerate any
# time, never hand-edit. tests/golden/ (the locked facit reference images)
# is NOT ignored — that's the versioned baseline.
tests/results/
